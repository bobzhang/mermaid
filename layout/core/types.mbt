/// A coordinate on the ASCII grid used for pathfinding.
pub type AsciiGridCoord = @layout_engine_core.AsciiGridCoord

/// A coordinate on the state diagram layout grid.
pub type LayoutStateGridCoord = @layout_engine_core.LayoutStateGridCoord
