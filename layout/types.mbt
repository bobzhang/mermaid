/// A coordinate on the ASCII grid used for pathfinding.
pub type AsciiGridCoord = @layout_core.AsciiGridCoord

/// A coordinate on the state diagram layout grid.
pub type LayoutStateGridCoord = @layout_core.LayoutStateGridCoord
